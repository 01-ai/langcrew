# === REQUIRED ===
OPENAI_API_KEY=  # Your OpenAI API key (used by agents and optionally by crawl4ai)
LINKEDIN_COOKIE=

# === WEB SEARCH TOOL (Required for search features) ===
LANGCREW_WEB_SEARCH_ENDPOINT=  # e.g., https://api.serper.dev/search
LANGCREW_WEB_SEARCH_API_KEY=   # Your search service API key

# === CRAWL4AI INTEGRATION (Optional) ===
# Web fetch tool uses crawl4ai service for content extraction
# If not configured, tool will automatically use pruning mode (no LLM required)
#
# LANGCREW_CRAWL4AI_SERVICE_URL=http://localhost:11235  # Crawl4ai service endpoint
# LANGCREW_CRAWL4AI_LLM_PROVIDER=openai/gpt-4o-mini    # LLM for content filtering
# LANGCREW_CRAWL4AI_LLM_API_KEY=                       # Defaults to OPENAI_API_KEY
