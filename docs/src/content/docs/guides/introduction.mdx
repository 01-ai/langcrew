---
title: Introduction to LangCrew
description: A high-level multi-agent framework built on LangGraph for enterprise-ready AI agent development
---

## What is LangCrew?

LangCrew is a **high-level multi-agent development framework** built on LangGraph that makes it easy to create, orchestrate, and monitor AI agent systems. It bridges the gap between research-grade multi-agent systems and production-ready applications.

## Why Choose LangCrew?

LangCrew provides three core advantages that set it apart:

- **🎯 Enterprise Ready**: Built-in human-in-the-loop approvals and audit trails for production environments
- **⚡ Developer Friendly**: CrewAI-compatible decorators enable seamless migration and rapid development
- **📊 Full-Stack Visualization**: Complete Agent-UI protocol with real-time monitoring and React components

### What You Get Out of the Box

✅ **Decorator-Based Development** - Familiar `@agent`, `@task`, `@crew` syntax  
✅ **YAML Configuration** - Externalize agent and task definitions  
✅ **Tool Approval Workflows** - Human oversight for sensitive operations  
✅ **Real-Time Monitoring** - Live execution streaming and debugging  
✅ **Memory Management** - Persistent context across agent interactions  
✅ **LangGraph Integration** - Native graph-based workflow orchestration  
✅ **Production Security** - Audit trails and approval mechanisms  

## Core Features

### 1. Familiar Development Experience

Start with the decorator syntax you already know:

```python
from langcrew import CrewBase, agent, task
from langcrew_tools import WebSearchTool, WebFetchTool

@CrewBase
class MarketingCrew:
    agents_config = "config/agents.yaml"
    tasks_config = "config/tasks.yaml"
    
    @agent
    def researcher(self):
        return Agent(
            config=self.agents_config["researcher"],
            tools=[WebSearchTool(), WebFetchTool()]
        )
    
    @task
    def market_analysis(self):
        return Task(
            config=self.tasks_config["market_analysis"],
            agent=self.researcher()
        )
```

### 2. Configuration-Driven Development

Keep your logic separate from configuration:

```yaml
# config/agents.yaml
researcher:
  role: "Market Research Analyst"
  goal: "Gather comprehensive market intelligence"
  backstory: "Expert in identifying market trends and opportunities"

# config/tasks.yaml
market_analysis:
  description: "Analyze market trends for {product}"
  expected_output: "Detailed market analysis report with key insights"
```

### 3. Human-in-the-Loop Controls

Add approval workflows where you need them:

```python
# Configure which tools require approval
hitl_config = HITLConfig(
    enabled=True,
    tools_requiring_approval=["WebSearchTool", "DatabaseTool"]
)

crew = Crew(agents=[agent], tasks=[tasks], hitl=hitl_config)
```

### 4. Real-Time Monitoring

Stream execution events to your frontend:

```python
# Stream agent execution in real-time
async for event in crew.astream_events(input_data):
    if event["event"] == "on_tool_start":
        print(f"Tool: {event['name']} started")
    elif event["event"] == "on_agent_finish":
        print(f"Agent completed: {event['data']['output']}")
```

## Migration from CrewAI

LangCrew provides **seamless migration** from existing CrewAI projects:

1. ✅ **Same Decorator Syntax** - Your `@agent`, `@task`, `@crew` decorators work unchanged
2. ✅ **Configuration Compatibility** - Existing YAML configs work out of the box
3. ✅ **Enhanced Capabilities** - Gain HITL, monitoring, and production features automatically

## Getting Started

Ready to build your first crew? Follow these steps:

1. **[Install LangCrew](/guides/installation)** - Set up your development environment
2. **[Quick Start Tutorial](/guides/quickstart)** - Build your first multi-agent system
3. **[Core Concepts](/concepts/agents)** - Understand agents, tasks, and crews
4. **[Production Guide](/guides/production)** - Deploy with monitoring and approvals

## Community and Support

- **GitHub**: [github.com/01-ai/langcrew](https://github.com/01-ai/langcrew)
- **Documentation**: Comprehensive guides and examples
- **Issues**: Report bugs and request features