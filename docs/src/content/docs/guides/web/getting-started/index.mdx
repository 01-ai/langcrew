---
title: Web - Getting Started
description: Get started with Web in 5 minutes
---

Quick guide to get Web server working with your langcrew application.

## Installation

Web is included with langcrew - no additional installation required:

```bash
uv add langcrew --prerelease=allow --index https://nexus.lingyiwanwu.net/repository/pypi-hosted/simple
```

## Basic Example

Here's a simple example that creates a web server for an agent:

```python
from langcrew import Agent
from langcrew.web import create_server

# Create an agent
agent = Agent(
    role="Web Assistant",
    goal="Help users through web interface",
    backstory="You are a helpful web-based AI assistant",
    verbose=True
)

# Create and start server
server = create_server(
    agents=[agent],
    host="0.0.0.0",
    port=8000,
    cors_origins=["http://localhost:3000"]
)

# Start the server
if __name__ == "__main__":
    print("Starting web server on http://localhost:8000")
    server.run()
```

## LangGraph Integration Example

For more advanced workflows with LangGraph:

```python
from langcrew import Agent, Crew
from langcrew.web import create_langgraph_server

# Create agents and crew
researcher = Agent(
    role="Research Specialist",
    goal="Gather information on topics",
    backstory="Expert researcher with web access"
)

writer = Agent(
    role="Content Writer", 
    goal="Create engaging content",
    backstory="Skilled writer who creates compelling content"
)

crew = Crew(agents=[researcher, writer])

# Create LangGraph-enabled server
server = create_langgraph_server(
    crew=crew,
    host="0.0.0.0",
    port=8000,
    enable_streaming=True,
    tool_display=True
)

# Start server
server.run()
```

## Streaming Example

Enable real-time streaming responses:

```python
from langcrew.web import AdapterServer, LangGraphAdapter

# Create adapter with streaming
adapter = LangGraphAdapter(
    crew=crew,
    enable_streaming=True,
    stream_tool_calls=True,
    tool_display=True
)

# Create server
server = AdapterServer(
    adapter=adapter,
    host="0.0.0.0",
    port=8000
)

server.run()
```

## Testing Your Setup

### Test with cURL

```bash
# Test basic chat endpoint
curl -X POST http://localhost:8000/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "Hello, can you help me with research?"}'

# Test task execution
curl -X POST http://localhost:8000/execute \
  -H "Content-Type: application/json" \
  -d '{"task": "research_topic", "input": {"topic": "AI trends"}}'
```

### Test with SSE (Server-Sent Events)

```javascript
// Connect to streaming endpoint using SSE
const eventSource = new EventSource('http://localhost:8000/api/v1/chat', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json'
    },
    body: JSON.stringify({
        message: 'Hello from SSE!',
        session_id: 'test-session'
    })
});

eventSource.onmessage = function(event) {
    const data = JSON.parse(event.data);
    console.log('Received:', data);
};

eventSource.onerror = function(error) {
    console.error('SSE error:', error);
};
```

### Run the Complete Example

```bash
# Set your OpenAI API key
export OPENAI_API_KEY=your_api_key_here

# Run from the web example directory
cd examples/components/web
uv run web_example.py
```

This example demonstrates:
- Basic HTTP server setup
- Agent integration with web endpoints
- Streaming responses
- Tool call visualization

## More Examples

For additional examples, check out:

- **📁 `examples/components/web/`** - Complete web server implementations
- **📄 `examples/components/web/README.md`** - Setup instructions  
- **🐍 `examples/components/web/web_example.py`** - Runnable server code
- **🌐 `examples/components/web/web_chat/`** - Full chat application

## Troubleshooting

### Server Won't Start

Check if port is available:
```bash
# Check if port 8000 is in use
lsof -i :8000

# Use different port if needed
server = create_server(port=8080)
```

### CORS Issues

Configure CORS origins properly:
```python
# ❌ Wrong - restrictive CORS
server = create_server(cors_origins=[])

# ✅ Correct - allow your frontend
server = create_server(
    cors_origins=["http://localhost:3000", "https://yourdomain.com"]
)
```

### Streaming Not Working

Ensure streaming is enabled:
```python
# Enable streaming in adapter
adapter = LangGraphAdapter(
    crew=crew,
    enable_streaming=True,
    stream_tool_calls=True
)
```

### Import Errors

Make sure you're importing from the correct modules:
```python
# Core web components
from langcrew.web import create_server, create_langgraph_server

# Advanced components
from langcrew.web import AdapterServer, LangGraphAdapter

# Protocol types
from langcrew.web import ChatRequest, StreamMessage
```

## Next Steps

- **[Configuration Guide](/guides/web/configuration)** - Learn all options
- **[Web Concepts](/concepts/web)** - Understand architecture
- **Examples** - Check `examples/components/web/` for implementations