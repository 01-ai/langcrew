---
title: 安装
description: 学习如何安装 langcrew 并设置您的开发环境
---

本指南将指导您安装 langcrew 并设置开发环境。

## 系统要求

在安装 langcrew 之前，请确保您具备：

- **Python**: 3.11 到 3.12 版本
- **pip**: 建议使用最新版本
- **虚拟环境**: 建议用于项目隔离

## 安装

> **注意**: langcrew 目前不是开源的，需要访问私有包索引。

使用 uv 直接从私有索引安装 langcrew：

```bash
uv add langcrew --prerelease=allow --index https://nexus.lingyiwanwu.net/repository/pypi-hosted/simple
```

### 可选：永久配置私有索引

如果您计划频繁使用 langcrew，可以在 `pyproject.toml` 中配置私有索引，避免每次都指定：

```toml
[tool.uv]
default-groups = ['dev']
extra-index-url = ["https://nexus.lingyiwanwu.net/repository/pypi-hosted/simple"]
```

添加此配置后，您可以简单运行：

```bash
uv add langcrew --prerelease=allow
```

### 开发安装

要贡献或修改 langcrew，您需要克隆仓库并设置开发环境：

```bash
git clone https://github.com/01-ai/langcrew.git
cd langcrew
```

仓库在其 `pyproject.toml` 中包含必要的私有索引配置。使用 uv 安装开发依赖：

```bash
# 使用 uv（推荐）
uv sync

# 或使用 pip 与私有索引
pip install -e . --extra-index-url https://nexus.lingyiwanwu.net/repository/pypi-hosted/simple
```

## 验证安装

```python
import langcrew
print(langcrew.__version__)

# 测试基本功能
from langcrew import Agent

agent = Agent(
    role="测试智能体",
    goal="验证安装",
    backstory="一个有用的测试智能体"
)
print(f"智能体已创建: {agent.role}")
```

## 故障排除

### 常见问题

**ImportError: No module named 'langcrew'**
<<<<<<< HEAD
- 确保您已激活虚拟环境
- 使用 `pip list | grep langcrew` 验证安装

**依赖冲突**
- 创建新的虚拟环境
- 使用 `pip install --upgrade langcrew`

### 获取帮助
=======

- Ensure you've activated your virtual environment
- Verify installation with `pip list | grep langcrew`

**API Key Errors**

- Check your `.env` file is in the correct location
- Verify environment variables are loaded: `echo $OPENAI_API_KEY`

**Dependency Conflicts**

- Create a fresh virtual environment
- Use `pip install --upgrade langcrew`
>>>>>>> 05fcc97d53f3e66a8215f57f55cd4dc92e91e3ab

如果遇到问题：

1. 搜索 [GitHub Issues](https://github.com/01-ai/langcrew/issues)
2. 查看文档获取更多示例

## 下一步

<<<<<<< HEAD
- **[快速开始](/zh/guides/quickstart)** - 构建您的第一个智能体
- **[核心概念](/zh/concepts/)** - 了解智能体、任务和团队
- **[示例](/examples/)** - 查看实际用例
=======
## Next Steps

Now that you have langcrew installed:

- Follow the [Quick Start Guide](/guides/quickstart) to build your first crew
- Explore [Core Concepts](/concepts/agents) to understand the framework
- Check out [Examples](/examples/game-builder) for inspiration
>>>>>>> 05fcc97d53f3e66a8215f57f55cd4dc92e91e3ab
